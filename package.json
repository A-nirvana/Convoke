{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Server && pnpm install && pnpm run build && cd ../Chat && pnpm install && pnpm run build && cd ..",
    "start": "cd Server && pnpm start"
  },
  "devDependencies": {},
  "dependencies": {}
}